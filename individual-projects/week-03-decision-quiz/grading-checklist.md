# Grading Checklist: Week 03 Decision Quiz

Branch to grade: `week-03-decision-quiz`

## Required Conditional Mastery

- [ ] Asks at least three questions with `input()`.
- [ ] Uses `.strip()` or `.lower()` on at least one text input before comparing it.
- [ ] Uses at least one equality comparison with `==`.
- [ ] Uses at least one inequality comparison with `!=`, `<`, `<=`, `>`, or `>=`.
- [ ] Uses at least one `if` / `else` decision.
- [ ] Uses at least one `if` / `elif` / `else` chain with three or more possible paths.
- [ ] Uses at least one multiple-condition expression with `and` or `or`.
- [ ] Tracks a score, category, or result variable that changes based on user answers.
- [ ] Prints a final result that depends on the score, category, or result variable.
- [ ] Includes at least three test cases in `notes.md`.

## Stretch 1

- [ ] Adds input validation for at least one question.
- [ ] Uses both `and` and `or` somewhere in the program.
- [ ] Adds at least two more test cases to `notes.md`.
- [ ] Keeps the required project working.

## Stretch 2

- [ ] Uses a ternary conditional expression.
- [ ] Adds a replay option or second round.
- [ ] Uses a nested conditional or helper function to keep code readable.
- [ ] Adds at least two more test cases to `notes.md`.
- [ ] Keeps the required project working.
