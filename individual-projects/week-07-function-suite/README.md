# Week 07: Function Suite

## Focus

return values, Boolean helpers, and simple tests.

## Textbook Grounding

Official textbook: *How to Think Like a Computer Scientist: Learning with Python 3*.

- Primary reading: Chapter 6, Fruitful functions
- Main ideas: return values, Boolean helpers, and simple tests
- Textbook link: <https://openbookproject.net/thinkcs/python/english3e/fruitful_functions.html>

## Branch Name

Use this exact feature branch name for this assignment:

```text
week-07-function-suite
```

Open your pull request from `week-07-function-suite` into your repo's `main` branch. Submit the PR link in Canvas when this assignment is collected.

## Required Friday Project

Write a small collection of useful functions and test their return values from `main()`.

The goal is not to build the largest possible program. The goal is to show that you can use this week's textbook concepts correctly and explain your choices.

## Requirements I Will Check

Your program must include all of these:

- [ ] Define at least four functions besides `main()`.
- [ ] At least three functions must return a value.
- [ ] At least one function must return `True` or `False`.
- [ ] At least one function must use a parameter in a calculation or comparison.
- [ ] Store returned values in variables before printing at least twice.
- [ ] Print labeled test output for each function.
- [ ] Avoid using `input()` inside the helper functions; collect input in `main()` if needed.
- [ ] Include at least three test cases in `notes.md`, including one edge case.

## Test Cases Required In `notes.md`

Include at least three tests like this:

```text
Test 1
Inputs or actions: what you typed, clicked, or changed
Expected result: what should happen
Actual result: what happened when you ran it
```

Your tests should show different paths or cases. Do not test the same behavior three times.

## Expected Result

A user can run your program, see clear output or interaction, and verify that the required concept is working.

## Stretch 1: Solid Extension

This is optional extra credit for students who have the required project working and want a manageable next step.

Requirements:

- [ ] Add at least two edge-case tests.
- [ ] Add one more Boolean helper function.
- [ ] Use an `if` statement in `main()` that depends on a returned Boolean value.
- [ ] Keep function names clear and specific.

## Stretch 2: Challenge Extension

This is optional extra credit for students who already know some programming and want a more demanding version of the same week's topic.

Requirements:

- [ ] Add a simple menu that lets the user choose which function to run.
- [ ] Keep the calculations inside functions, not inside the menu logic.
- [ ] Use a loop so the menu can run more than once.
- [ ] Add two more test cases for menu choices.

## Submission Checklist

- [ ] My code runs before I submit.
- [ ] My branch name is exactly `week-07-function-suite`.
- [ ] My `notes.md` includes at least three required test cases.
- [ ] My pull request is open into my repo's `main` branch.
- [ ] I submitted the PR link in Canvas.

## Files

- `starter.py`: start here
- `notes.md`: include planning notes and required test cases
